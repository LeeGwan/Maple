#include "DrawManager.h"
#include "../../../Global.h"

// ------------------------------------------------------------
// 생성자
// ------------------------------------------------------------
DrawManager::DrawManager()
{
    // 초기화가 필요할 경우 여기에 작성
}

// ------------------------------------------------------------
// 초기화 함수 (현재는 비어 있음)
// 추후 텍스처 로딩, 초기 상태 세팅 등에 사용 가능
// ------------------------------------------------------------
void DrawManager::DrawManager_Init()
{
    // 예) G_ctx->MakeRender->LoadCommonTextures();
}

// ------------------------------------------------------------
// 메인 Draw 루프 - 프레임마다 호출
// ------------------------------------------------------------
void DrawManager::Draw()
{
    // 현재는 객체 렌더링만 처리
    G_ctx->G_ObjectManager->Update();

    // 향후 확장 가능 영역 예시:
    // Draw_Background();
    // Draw_Objects();
    // Draw_UI();
    // Draw_DebugInfo();
}
